package modularization.libraries.uicomponents;

import android.content.Context;
import android.util.AttributeSet;

import androidx.appcompat.widget.AppCompatSpinner;

public class MagicalSpinner extends AppCompatSpinner {

    public MagicalSpinner(Context context) {
        super(context);
    }

    public MagicalSpinner(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public MagicalSpinner(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }
}
